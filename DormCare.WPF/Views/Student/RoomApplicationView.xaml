<UserControl x:Class="DormCare.WPF.Views.Student.RoomApplicationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource HeaderCard}" Margin="0,0,0,16">
            <DockPanel>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="📝 ĐĂNG KÝ PHÒNG KÝ TÚC XÁ" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="Tra cứu phòng trống, lựa chọn phòng phù hợp và nộp đơn nguyện vọng" FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main Body Grid -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2fr"/>
                <ColumnDefinition Width="1fr"/>
            </Grid.ColumnDefinitions>

            <!-- Room Selection DataGrid -->
            <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,12,0">
                <DockPanel Margin="16">
                    <TextBlock DockPanel.Dock="Top" Text="Danh Sách Phòng Cần Tuyển Sinh Viên" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                    
                    <DataGrid ItemsSource="{Binding AvailableRooms}" AutoGenerateColumns="False" IsReadOnly="True" Style="{StaticResource ModernDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Phòng" Binding="{Binding RoomNumber}" Width="80"/>
                            <DataGridTextColumn Header="Tòa Nhà" Binding="{Binding BuildingName}" Width="120"/>
                            <DataGridTextColumn Header="Loại Phòng" Binding="{Binding RoomType}" Width="100"/>
                            <DataGridTextColumn Header="Đối Tượng" Binding="{Binding GenderType}" Width="90"/>
                            <DataGridTextColumn Header="Giường Trống" Binding="{Binding AvailableBeds}" Width="100"/>
                            <DataGridTextColumn Header="Giá Thuê/Tháng" Binding="{Binding MonthlyRent, StringFormat={}{0:N0} VNĐ}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <!-- Application Form & History -->
            <Border Grid.Column="1" Style="{StaticResource CardBorder}" Margin="12,0,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16">
                        <TextBlock Text="Nộp Đơn Nguyện Vọng Mới" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>

                        <TextBlock Text="Mã Phòng Mong Muốn:" FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="A101" Style="{StaticResource FormTextBox}" Margin="0,0,0,12"/>

                        <TextBlock Text="Mã Giường (Tùy chọn):" FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="A101-B1" Style="{StaticResource FormTextBox}" Margin="0,0,0,12"/>

                        <TextBlock Text="Lý Do Nộp Đơn:" FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="Mong muốn ở ký túc xá để tiện việc học tập và sinh hoạt" Style="{StaticResource FormTextBox}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,16"/>

                        <Button Content="📝 Gửi Đơn Đăng Ký" Style="{StaticResource PrimaryButton}" Height="40" FontSize="14" Margin="0,0,0,24"/>

                        <Separator Background="{StaticResource CardBorderBrush}" Margin="0,0,0,16"/>

                        <TextBlock Text="Lịch Sử Đơn Đăng Ký Của Bạn" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                        <Border Background="#F8FAFC" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12">
                            <StackPanel>
                                <DockPanel Margin="0,0,0,4">
                                    <TextBlock Text="Mã đơn: APP-2026-0005" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    <Border HorizontalAlignment="Right" Background="#FEF3C7" CornerRadius="4" Padding="6,2">
                                        <TextBlock Text="Chờ xét duyệt" Foreground="#D97706" FontWeight="Bold" FontSize="11"/>
                                    </Border>
                                </DockPanel>
                                <TextBlock Text="Phòng xin: A101 (Tòa A)" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                                <TextBlock Text="Ngày nộp: 26/07/2026" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
