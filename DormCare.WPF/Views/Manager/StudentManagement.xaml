<UserControl x:Class="DormCare.WPF.Views.Manager.StudentManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Panel -->
        <Border Grid.Row="0" Style="{StaticResource HeaderCard}" Margin="0,0,0,16">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="➕ Thêm mới" Style="{StaticResource PrimaryButton}" Command="{Binding AddStudentCommand}" Margin="0,0,8,0"/>
                    <Button Content="✏️ Sửa đổi" Style="{StaticResource SecondaryButton}" Command="{Binding EditStudentCommand}" Margin="0,0,8,0"/>
                    <Button Content="❌ Xóa" Style="{StaticResource SmallDangerButton}" Command="{Binding DeleteStudentCommand}" Height="36" Padding="12,0" Margin="0,0,12,0"/>
                    <Button Content="🔑 Nhận phòng (Check-in)" Style="{StaticResource PrimaryButton}" Command="{Binding CheckInCommand}" Margin="0,0,8,0"/>
                    <Button Content="🚪 Trả phòng (Check-out)" Style="{StaticResource SecondaryButton}" Command="{Binding CheckOutCommand}" Margin="0,0,12,0"/>
                    <Button Content="🔄 Tải lại" Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}"/>
                </StackPanel>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="👥 QUẢN LÝ SINH VIÊN KÝ TÚC XÁ" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="Quản lý hồ sơ học viên, thông tin lưu trú, nhận/trả phòng ký túc xá" FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Search & Filter Bar -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,16" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="🔍 Tìm kiếm sinh viên (Họ tên / Mã SV / Ngành học / Lớp)" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2">
                    <TextBlock Text="🚪 Xem Theo Phòng" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding Rooms}" SelectedItem="{Binding SelectedRoomFilter}" Height="38" Style="{StaticResource ModernComboBox}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding RoomNumber}" Foreground="#0F172A" FontSize="13"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Student DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource CardBorder}">
            <DataGrid ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent}" Style="{StaticResource ModernDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã SV" Binding="{Binding StudentCode}" Width="110"/>
                    <DataGridTextColumn Header="Họ và Tên" Binding="{Binding FullName}" Width="180"/>
                    <DataGridTextColumn Header="Ngành Học" Binding="{Binding Major}" Width="170"/>
                    <DataGridTextColumn Header="Lớp" Binding="{Binding ClassName}" Width="100"/>
                    <DataGridTextColumn Header="Giới Tính" Binding="{Binding Gender}" Width="90"/>
                    <DataGridTextColumn Header="Số Điện Thoại" Binding="{Binding PhoneNumber}" Width="130"/>
                    <DataGridTextColumn Header="Tòa Nhà" Binding="{Binding BuildingName}" Width="130"/>
                    <DataGridTextColumn Header="Phòng" Binding="{Binding RoomNumber}" Width="100"/>
                    <DataGridTextColumn Header="Giường" Binding="{Binding BedNumber}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
