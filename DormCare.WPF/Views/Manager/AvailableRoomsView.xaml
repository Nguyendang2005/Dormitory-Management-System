<UserControl x:Class="DormCare.WPF.Views.Manager.AvailableRoomsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:DormCare.WPF.Converters">

    <UserControl.Resources>
        <conv:StatusToColorConverter x:Key="StatusColorConverter"/>
        <conv:StatusToVietnameseConverter x:Key="StatusVietnameseConverter"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="🔍 Tìm Phòng Còn Chỗ" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="Tìm kiếm các phòng ở ký túc xá đang có giường trống để tiếp nhận sinh viên mới" FontSize="14" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="🔄 Đặt Lại Bộ Lọc" Style="{StaticResource PrimaryButton}" Command="{Binding ResetFiltersCommand}" Margin="0,0,10,0"/>
                <Button Content="⚡ Tải Lại" Style="{StaticResource PrimaryButton}" Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </DockPanel>

        <!-- Multi-Criteria Filter Bar -->
        <Border Grid.Row="1" Style="{StaticResource CardFrame}" Margin="0,0,0,16" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.2*"/>
                </Grid.ColumnDefinitions>

                <!-- Room Type Filter -->
                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Loại Phòng" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                    <ComboBox SelectedValue="{Binding SelectedRoomType, Mode=TwoWay}" SelectedValuePath="Tag" Height="38" Background="#0F172A" Foreground="White" BorderBrush="{StaticResource CardBorderBrush}">
                        <ComboBoxItem Content="Tất cả loại phòng" Tag="All" IsSelected="True"/>
                        <ComboBoxItem Content="Tiêu chuẩn (Standard)" Tag="Standard"/>
                        <ComboBoxItem Content="Cao cấp (Premium)" Tag="Premium"/>
                        <ComboBoxItem Content="Hỗ trợ (Accessible)" Tag="Accessible"/>
                    </ComboBox>
                </StackPanel>

                <!-- Gender Filter -->
                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                    <TextBlock Text="Giới Tính Phòng" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                    <ComboBox SelectedValue="{Binding SelectedGenderType, Mode=TwoWay}" SelectedValuePath="Tag" Height="38" Background="#0F172A" Foreground="White" BorderBrush="{StaticResource CardBorderBrush}">
                        <ComboBoxItem Content="Tất cả giới tính" Tag="All" IsSelected="True"/>
                        <ComboBoxItem Content="Nam (Male)" Tag="Male"/>
                        <ComboBoxItem Content="Nữ (Female)" Tag="Female"/>
                        <ComboBoxItem Content="Hỗn hợp (Mixed)" Tag="Mixed"/>
                    </ComboBox>
                </StackPanel>

                <!-- Min Available Beds -->
                <StackPanel Grid.Column="2" Margin="0,0,12,0">
                    <TextBlock Text="Giường Trống Tối Thiểu" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding MinAvailableBeds, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!-- Max Rent -->
                <StackPanel Grid.Column="3">
                    <TextBlock Text="Giá Thuê Tối Đa (VNĐ)" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding MaxMonthlyRent, StringFormat='{}{0:N0}', UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Data Grid Results -->
        <Border Grid.Row="2" Style="{StaticResource CardFrame}">
            <Grid>
                <!-- Loading or Empty State Message -->
                <TextBlock Text="{Binding LoadingStateMessage}"
                           Visibility="{Binding IsEmptyState, Converter={StaticResource BoolToVisConverter}}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontSize="16" Foreground="{StaticResource TextMutedBrush}"/>

                <!-- Available Rooms Table -->
                <DataGrid ItemsSource="{Binding AvailableRooms}" AutoGenerateColumns="False" CanUserAddRows="False" RowHeight="42">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Số Phòng" Binding="{Binding RoomNumber}" Width="110" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Tòa Nhà" Binding="{Binding BuildingName}" Width="140"/>
                        <DataGridTextColumn Header="Tầng" Binding="{Binding FloorNumber}" Width="80"/>
                        <DataGridTextColumn Header="Loại Phòng" Binding="{Binding RoomType, Converter={StaticResource StatusVietnameseConverter}}" Width="120"/>
                        <DataGridTextColumn Header="Sức Chứa" Binding="{Binding Capacity}" Width="90"/>
                        <DataGridTextColumn Header="Đã Ở" Binding="{Binding OccupiedBeds}" Width="80"/>
                        <DataGridTextColumn Header="Còn Trống" Binding="{Binding AvailableBeds}" Width="100" FontWeight="Bold" Foreground="{StaticResource KinpakuGoldBrush}"/>
                        <DataGridTextColumn Header="Tình Trạng Chỗ Ở" Binding="{Binding OccupancyText}" Width="220"/>
                        <DataGridTextColumn Header="Giá Thuê / Tháng" Binding="{Binding MonthlyRent, StringFormat='{}{0:N0} VNĐ'}" Width="160" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Đối Tượng" Binding="{Binding GenderType, Converter={StaticResource StatusVietnameseConverter}}" Width="100"/>
                        <DataGridTemplateColumn Header="Trạng Thái" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding Status, Converter={StaticResource StatusColorConverter}}"
                                            CornerRadius="12" Padding="10,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Status, Converter={StaticResource StatusVietnameseConverter}}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
